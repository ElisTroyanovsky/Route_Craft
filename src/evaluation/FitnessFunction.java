package evaluation;
/**
 * Computes solution quality (lower is better) used by optimizers.
 * Usually: totalDistance + penalties(constraints violations).
 *
 * TODO:
 * - Define fitness formula and ensure it is deterministic.
 * - Support different fitness modes (distance only, distance + balance).
 * - Integrate PenaltyCalculator.
 */

public class FitnessFunction {
}
