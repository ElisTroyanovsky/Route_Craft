package validation;
/**
 * Validates solution feasibility against Problem and Constraints.
 * Used for debugging, testing, and (optionally) strict feasibility checks.
 *
 * TODO:
 * - Check each customer visited exactly once.
 * - Check routes start/end at depot (explicit or implicit).
 * - Check constraints: capacity, maxDistance, time windows (future).
 * - Provide detailed validation errors.
 */
public class SolutionValidator {
}
