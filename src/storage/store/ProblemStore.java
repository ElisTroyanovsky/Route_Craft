package storage.store;
/**
 * Storage facade for Problem objects.
 * Hides underlying database (Firebase/MSSQL/file) behind a simple API.
 *
 * TODO:
 * - Define CRUD methods (save, load, list, delete).
 * - Enforce user scoping (userId ownership).
 * - Use mapper to convert domain <-> record/entity.
 */
public class ProblemStore {
}
