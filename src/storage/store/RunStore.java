package storage.store;
/**
 * Storage facade for algorithm run statistics (RunStats/RunRecord).
 * Keeps experiment history: fitness, duration, iterations, parameters.
 *
 * TODO:
 * - Define RunStats model and persistence format.
 * - Store penalty breakdown and best-so-far progress (optional).
 * - Enable comparisons across algorithms and parameter sets.
 */
public class RunStore {
}
