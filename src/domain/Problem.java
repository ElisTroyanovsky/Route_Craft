package domain;

/**
 * One optimization scenario: depot + customers + fleet + constraints.
 * Acts as the single input object for any Optimizer implementation.
 *
 * TODO:
 * - Validate input (non-empty customers, fleet size > 0).
 * - Provide a stable indexing of locations for DistanceMatrix mapping.
 * - Add builder/factory for demo problems and tests.
 */

public class Problem {
}
