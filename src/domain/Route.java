package domain;
/**
 * Single vehicle route: an ordered list of stops assigned to one truck.
 * Typically starts/ends at the depot (explicitly or implicitly).
 *
 * TODO:
 * - Decide representation: include depot in stops or treat it implicitly.
 * - Add cached length/time fields and a recompute() method.
 * - Add helper operations: swap, reverse segment, insert, remove.
 */
public class Route {
}
