package domain;

/**
 * Problem constraints (rules) that define feasibility and penalties.
 * Kept separate from optimizer logic to avoid hard-coded rules inside algorithms.
 *
 * TODO:
 * - Add toggles: capacityEnabled, maxDistanceEnabled, timeWindowsEnabled.
 * - Add numeric limits: maxDistancePerTruck, truckCapacity, etc.
 * - Define penalty weights (or keep them in PenaltyCalculator config).
 */

public class Constraints {
}
