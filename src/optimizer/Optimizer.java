package optimizer;
/**
 * Optimization engine contract.
 * Implementations (GA/ACO/etc.) take a Problem and DistanceMatrix and return the best Solution found.
 *
 * TODO:
 * - Define method signature and return type (Solution or RunResult).
 * - Add optional hooks: setSeed(), setListener(), getStats().
 */

public class Optimizer {
}
